{
  "name": "tracksheet",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --filter '*' dev",
    "dev:client": "bun run --filter @tracksheet/client dev",
    "dev:server": "bun run --filter @tracksheet/server dev",
    "build": "cd apps/client && npm install --include=dev && npx vite@5.4.21 build && cd ../../ && cd apps/server && npm install --legacy-peer-deps && npm run build",
    "build:client": "cd apps/client && npm install --include=dev && npx vite@5.4.21 build",
    "build:server": "cd apps/server && npm install --legacy-peer-deps && npm run build",
    "lint": "bun run --filter '*' lint",
    "typecheck": "bun run --filter '*' typecheck"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "eslint": "^8.57.0",
    "typescript": "^5.4.0"
  }
}
